import random

random.randint(1, 6)
random.randint(1000, 9999)
random.sample(lst, 2)
n = random.randint(0, 100); n % 2 == 0
"".join(random.choice("abcdefghi") for _ in range(10))
n = random.randint(1, 100); "katta" if n > 50 else n
random.choice(["heads", "tails"])
n = random.randint(1, 20); n % 3 == 0
el = random.choice(lst); lst.remove(el)
n = random.randint(1, 1000); n * n
